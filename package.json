{
  "name": "water-meter-check",
  "version": "1.0.0",
  "description": "Ingest and command server for CAT-1 NB-IoT water meters over raw TCP",
  "type": "module",
  "main": "dist/src/server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "start": "node dist/src/server.js",
    "dev": "tsc --watch --preserveWatchOutput & node --watch dist/src/server.js",
    "test": "npm run clean && tsc && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^9.9.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
